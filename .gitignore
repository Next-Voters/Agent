.venv
.env
__pycache__
.idea
.ruff_cache
email_failures.json

# OS noise
.DS_Store

# Frontend toolchain (built output in api/static/dist IS committed)
frontend/node_modules/
# Vercel-only build output (`npm run build:vercel`); the FastAPI-served bundle stays in api/static/dist
frontend/dist/
