# Worker Lambda dependencies.
# The scaffold handler only uses the standard library; add packages here as the
# real job logic is implemented (they are installed into the Lambda task root by
# docker/Dockerfile.worker).
